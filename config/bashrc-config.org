#+TITLE: BASH Config
#+AUTHOR: Denis Furmann
#+DESCRIPTION: Literate config for Bash shell

* Prompt theme
** Colors definitions
#+begin_src bash :tangle "~/.bashrc"
COLOR_GREEN=$(tput setaf 150)  # Green
COLOR_MAUVE=$(tput setaf 141)  # Mauve
COLOR_RESET=$(tput sgr0)       # Reset to default color
#+end_src
** Parts functions
#+begin_src bash :tangle "~/.bashrc"
  prompt_working_directory() {
      echo -n "${COLOR_MAUVE}\w"
  }
  prompt_git_branch() {
        local branch
        branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)
        if [ -n "$branch" ]
        then
            echo -n " ${COLOR_GREEN}(${branch})"
        fi
  }
#+end_src
** Final prompt assembly
#+begin_src bash :tangle "~/.bashrc"
assemble_prompt() {
    PS1="$(prompt_working_directory)$(prompt_git_branch)\n${COLOR_RESET}$ "
}
PROMPT_COMMAND=assemble_prompt

#+end_src

* ENV

#+begin_src bash :tangle "~/.bashrc"
  export PATH=$PATH:~/.cargo/bin

  export OPENCODE_API_KEY=sk-jUUQa2Sqmt6JOcW5WpKYszV8wZjM20Ae7e7SQHXma2CdV3RIvMAyF4gOLIPyYAAn
#+end_src



* Plugins setups
** FZF
#+begin_src bash :tangle "~/.bashrc"
if [ -f /usr/bin/fzf ]
then
  eval "$(fzf --bash)"
else
  echo "[INFO] fzf not installed!"
fi
#+end_src

** Fastfetch
#+begin_src bash :tangle "~/.bashrc" 
  if [ -f /usr/bin/fastfetch ]
  then
      fastfetch
  else
      echo "[INFO] fastfetch is not installed"
  fi
#+end_src

** Bash-Autocompletion
#+begin_src bash :tangle "~/.bashrc" 
  if [ -f /usr/share/bash-completion/bash_completion ]
  then
      . /usr/share/bash-completion/bash_completion
  else
      echo "[INFO] bash-completion is not installed"
  fi


** BAT
#+begin_src bash :tangle "~/.bashrc"
  export MANPAGER="sh -c 'sed -u -e \"s/\\x1B\[[0-9;]*m//g; s/.\\x08//g\" | bat -p -lman'"
#+end_src

* Aliases
#+begin_src bash :tangle "~/.bashrc"
  alias ls="eza --color=auto"
  alias ll="eza -la --color=auto --icons --group-directories-first --group"
  alias pinstall="sudo xbps-install -Su"
  alias pquery="xbps-query -Rs"
  alias void-update="sudo xbps-install -Syu"
  alias ..="cd .."
  alias ...="cd ../.."
  alias ....="cd ../../.."
  alias fzf="fzf --style full --preview 'bat --color=always {}' --bind 'focus:transform-header:file --brief {}'"
  alias cat="bat"
#+end_src
